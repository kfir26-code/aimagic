{
  "name": "aimagic",
  "version": "1.0.0",
  "private": true,
  "description": "AI Magic landing page (kfirperez.com) — static site",
  "scripts": {
    "dev": "live-server --port=8080 --wait=300"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
